[project]
name = "biocontacts"
version = "0.1.0"
description = "Tool for working with molecular interactions."
authors = [
    {name = "Ilia Bushmakin", email = "im.bushmakin@gmail.com"},
]
dependencies = [
    "padata",
    "scipy<=2.0.0",
]
requires-python = ">3.12.1"
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
